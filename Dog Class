/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package words;

/**
 *
 * @author callmedaddy
 */
public class Dog {
    
   String name = "bart";
   String title;
   String author;
   
   static String time;

   private int size;
   int r = 0;

   
   void bark() {
       System.out.println(name + " says Ruff");
   }

   public void setSize(int s) {
       
        size = r;
    }
    
   public int getSize(){
       
       return size;
   }
   
   
   
   
   
   int calArea( int heigth, int width){
       
       
     
       return heigth*width;
       
    
   }
   
   public static void setTime(String  t) {
  
       time = t;
   }
   
   public static String getTime(){
       
       return time;
   
   }
   void setArea(){
        area = (height * length) / 2;
    }
     

   
     
   /*
   
   declarar 
   
   x = 0;
   y = x + y;
   
   un ciclo for para que cuenta las veces 
   que se presiona el boton
   si
   supera los 3 6 9 
   cambia de color por cada vez que supere
   esos 3 niveles
   
   
   */
    /*
        int counter = 0;
        
        int count = 0;


     
        int x = 0;
        
        
        
        public int maybeNew (int index) {
            
            if ( index < 7) {
                
                Dog m4 = new Dog();
                m4.counter =  m4.counter + 1;
                return 1;
                
            } 
            
            return 0;
        }
        
        */
          /*
        void go(){
            
            Dog t = new Dog();
            int foo = 7;
            
            t.takeTwo(foo);
        }
        
        void takeTwo(int x){
            
            int z = x;
            
            System.out.println("Z is" + z);
        }
        
        */
        
         /*
           void setTimes(String t ){
               times = t;
           }
           
           String getTimes(){ // recuerda que el valor que estas pidiendo es String
               return times; // como void int o boolean
           } 
            */
            
             /*
        public int doStuff(int factor){
            
            if (ivar > 100){
                return ivar * factor; // esta bien multiplicacar pero es factor
            } else {
                return ivar * (5-factor); // 5-factor
            }
            
        }
    
    */
    
   
    
}
